# Remembrance Oracle — Swarm API Keys
# Copy this file to .env and fill in your keys.
# .env is gitignored — your keys stay local.

# Required (at least one)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional
GOOGLE_API_KEY=
GROK_API_KEY=
DEEPSEEK_API_KEY=

# Ollama (local, no key needed — just set host if non-default)
# OLLAMA_HOST=http://localhost:11434
