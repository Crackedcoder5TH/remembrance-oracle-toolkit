"use client";
import Link from "next/link";import{useEffect,useState}from"react";import{MetricCard,Panel,PortalShell}from"../../components/portal-shell";
type Data={acknowledgement:null|{version:string;acknowledgedAt:string;active:boolean};leads:Array<{leadId:string;submittedAt:string;sourcePage:string;disclosureVersion:string;consentTimestamp:string}>};
const responsibilities=["Contact leads only for the purpose they requested.","Respect opt-outs immediately.","Do not contact anyone marked Do Not Contact.","Do not share, resell, export, or misuse lead information.","Keep client information private.","Update lead status honestly.","Report bad leads or opt-outs through the portal.","Follow all applicable laws, carrier rules, and licensing requirements."];
export default function AgentCompliance(){const[data,setData]=useState<Data|null>(null);const[busy,setBusy]=useState(false);const[error,setError]=useState("");const load=()=>fetch("/api/agent/compliance",{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.message);setData(d)}).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function acknowledge(){setBusy(true);const r=await fetch("/api/agent/compliance",{method:"POST"});const d=await r.json();if(r.ok)setData(v=>v?{...v,acknowledgement:d.acknowledgement}:v);else setError(d.message);setBusy(false)}return <PortalShell role="agent" eyebrow="Responsible lead handling" title="Compliance" description="Every lead must be handled with care, professionalism, and respect. This page summarizes important reminders for working Valor Legacies leads.">{error&&<p className="mb-4 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</p>}<div className="grid gap-3 sm:grid-cols-3"><MetricCard label="Acknowledgement" value={data?.acknowledgement?.active?"Current":"Action needed"} note={data?.acknowledgement?`Recorded ${new Date(data.acknowledgement.acknowledgedAt).toLocaleDateString()}`:"Required before purchasing"} urgent={!data?.acknowledgement}/><MetricCard label="Purchased lead records" value={data?.leads.length??"—"} note="Consent summaries available"/><MetricCard label="Privacy-minded operations" value="Always" note="Protect consumer information"/></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><Panel title="Agent Responsibilities"><ul className="space-y-3 text-sm leading-6">{responsibilities.map(x=><li key={x} className="flex gap-3"><span className="text-[#c9a75f]">◆</span>{x}</li>)}</ul></Panel><Panel title="Compliance acknowledgement"><p className="text-sm leading-7 text-[#62594e]">I understand that leads must be handled professionally, privately, and only for their intended purpose.</p>{data?.acknowledgement?<p className="mt-5 rounded-xl bg-[#edf6f1] p-4 text-sm text-[#176b65]">Acknowledged on {new Date(data.acknowledgement.acknowledgedAt).toLocaleString()}.</p>:<button disabled={busy} onClick={acknowledge} className="mt-5 rounded-xl bg-[#176b65] px-5 py-3 font-semibold text-white disabled:opacity-50">{busy?"Recording…":"Acknowledge responsibilities"}</button>}</Panel><div className="lg:col-span-2"><Panel title="Purchased Lead Consent Summary">{data?.leads.length?<div className="overflow-x-auto"><table className="w-full min-w-[680px] text-sm"><thead><tr className="text-left text-xs uppercase text-[#776e61]"><th className="p-3">Lead</th><th>Submitted</th><th>Source page</th><th>Disclosure</th><th>Consent timestamp</th></tr></thead><tbody>{data.leads.map(l=><tr key={l.leadId} className="border-t border-[#e2d9c9]"><td className="p-3"><Link className="text-[#176b65]" href={`/agent/leads/${l.leadId}`}>{l.leadId}</Link></td><td>{new Date(l.submittedAt).toLocaleString()}</td><td>{l.sourcePage||"—"}</td><td>{l.disclosureVersion}</td><td>{l.consentTimestamp?new Date(l.consentTimestamp).toLocaleString():"Review required"}</td></tr>)}</tbody></table></div>:<p className="text-sm text-[#8a8175]">Consent summaries appear here only after a lead has been delivered to your account.</p>}</Panel></div><div className="lg:col-span-2"><Panel title="Opt-Out / Do-Not-Contact Instructions"><p className="text-sm leading-7 text-[#62594e]">If someone says “stop texting me,” “stop calling me,” “remove me,” “wrong number,” or “do not contact me again,” stop contact immediately and record the request on the authorized lead.</p><div className="mt-4 flex flex-wrap gap-2"><Link href="/agent/leads" className="rounded-xl bg-[#176b65] px-4 py-2 text-sm text-white">Mark Lead Do Not Contact</Link><Link href="/agent/leads" className="rounded-xl border border-[#e2d9c9] bg-white px-4 py-2 text-sm">Report Opt-Out</Link><Link href="/agent/support" className="rounded-xl border border-[#e2d9c9] bg-white px-4 py-2 text-sm">Contact Support</Link></div></Panel></div></div></PortalShell>}
