"use client";
import {useEffect,useState} from "react";import Link from "next/link";
type Lead={leadId:string;firstName:string;lastName:string;phone:string;email:string;state:string;dateOfBirth:string;coverageInterest:string;purchaseIntent:string;veteranStatus:string;createdAt:string;consentSummary:string};
const opener=(l:Lead)=>l.coverageInterest==="final-expense"?`Hi ${l.firstName}, this is [Agent]. I’m following up because you requested information about final expense planning. My goal is to help you understand simple options that may help keep that burden off your family.`:l.coverageInterest.includes("mortgage")?`Hi ${l.firstName}, this is [Agent]. I’m following up on your request about protecting your home. I’ll keep this simple and help you understand your options.`:l.veteranStatus?.includes("veteran")?`Hi ${l.firstName}, this is [Agent]. I’m following up because you requested guidance as a veteran or military family member. My goal is to help you understand what benefits may cover and where private protection may help fill the gap.`:`Hi ${l.firstName}, this is [Agent]. I’m following up on your request for guidance. My job is to help you understand what protection may fit this chapter.`;
export default function LeadDetail({params}:{params:{id:string}}){const [lead,setLead]=useState<Lead|null>(null),[error,setError]=useState("");useEffect(()=>{fetch(`/api/client/leads/${encodeURIComponent(params.id)}`).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.message);setLead(d.lead)}).catch(e=>setError(e.message))},[params.id]);if(error)return <main className="mx-auto max-w-3xl p-6"><Link href="/agent/leads">← My Leads</Link><p className="mt-8 rounded-xl border border-red-200 bg-red-50 p-5">{error}</p></main>;if(!lead)return <main className="p-8">Loading authorized lead…</main>;return <main className="mx-auto max-w-5xl p-4 py-8 sm:p-8"><Link href="/agent/leads" className="text-[#176b65]">← My Leads</Link><div className="mt-5 flex flex-wrap justify-between gap-4"><div><p className="text-sm text-[#176b65]">New · {lead.leadId}</p><h1 className="text-3xl font-semibold">{lead.firstName} {lead.lastName}</h1><p className="text-[#685f52]">{lead.state} · {lead.coverageInterest}</p></div><div className="flex gap-2"><a href={`tel:${lead.phone}`} className="rounded-xl bg-[#176b65] px-4 py-3 text-white">Call</a><a href={`sms:${lead.phone}`} className="rounded-xl border px-4 py-3">Text</a><a href={`mailto:${lead.email}`} className="rounded-xl border px-4 py-3">Email</a></div></div><div className="mt-7 grid gap-5 md:grid-cols-2"><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Contact & request</h2><dl className="mt-4 space-y-3 text-sm"><div><dt className="text-[#756b5e]">Phone</dt><dd>{lead.phone}</dd></div><div><dt className="text-[#756b5e]">Email</dt><dd>{lead.email}</dd></div><div><dt className="text-[#756b5e]">DOB</dt><dd>{lead.dateOfBirth||"Not provided"}</dd></div><div><dt className="text-[#756b5e]">Readiness</dt><dd>{lead.purchaseIntent||"Exploring"}</dd></div><div><dt className="text-[#756b5e]">Submitted</dt><dd>{new Date(lead.createdAt).toLocaleString()}</dd></div></dl></section><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Suggested opener</h2><p className="mt-3 text-sm leading-6">“{opener(lead)}”</p></section><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Consent proof</h2><p className="mt-3 text-sm">{lead.consentSummary}</p><p className="mt-4 rounded-lg bg-amber-50 p-3 text-sm">Honor contact preferences, identify yourself clearly, and stop outreach immediately after an opt-out.</p></section><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Activity & follow-up</h2><p className="mt-3 text-sm text-[#685f52]">Purchase recorded. CRM notes, appointments, and status persistence require the Phase 3 operations fields.</p><div className="mt-4 flex flex-wrap gap-2"><button className="rounded-lg border px-3 py-2">Add note</button><button className="rounded-lg border px-3 py-2">Set follow-up</button><button className="rounded-lg border px-3 py-2">Update status</button></div></section></div></main>}
