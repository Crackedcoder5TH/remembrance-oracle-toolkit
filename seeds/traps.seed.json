{
 "_README": "Recorded misreads. Each entry is a mistake that was actually made against this codebase, with the truth that corrects it. `brief` prints the matching traps BEFORE anything else, so the correction arrives before the call rather than after the wrong result. Append when a misread is found; never delete — a trap that stops firing is a trap someone will re-enter.",
 "traps": [
  {
   "match": [
    "void_compressor",
    "AdaptiveVoidCompressor",
    "compress(",
    "compress_signal",
    "void"
   ],
   "severity": "high",
   "wrong": "Calling compress() with text (source code, HTML, transcripts) and reading the result as a Void compression ratio.",
   "truth": "compress() does np.frombuffer(data, uint8) and matches the input against 78,853 pattern WAVEFORMS. Its docstring says 'whole-SIGNAL recursive'. Text read as a waveform is high-frequency noise, every fractal candidate loses, and the zlib fallback wins — so the number you get measures the fallback, not the substrate.",
   "tell": "_auto_method comes back 'void_symbolic' or 'raw_zlib' on every input. On real signal the fractal path wins: v4_chunked won 520/520 NAB and substrate windows.",
   "correct": "Numeric series → POST /compress_signal on compressor_service.py, which quantises the way goggle-web.js does. Never /compress, which utf-8 encodes and hands over the ASCII of a waveform."
  },
  {
   "match": [
    "compressor_service",
    "service",
    "8765"
   ],
   "severity": "high",
   "wrong": "Assuming a cold in-process AdaptiveVoidCompressor is the way to call the compressor, and concluding it is unusably slow (~114 s).",
   "truth": "compressor_service.py holds the library warm. A read is ~1.5 s against ~114 s cold — 75x. It also loads the substrate-fractal Rust index (45,547 patterns in 0.5 s) when the wheel is built.",
   "tell": "A per-call cost measured in tens of seconds means the library is being reloaded per call.",
   "correct": "Start it once: python3 compressor_service.py --host 127.0.0.1 --port 8765. Build the wheel with maturin if substrate_fractal will not import."
  },
  {
   "match": [
    "field",
    "contribute",
    "coherence",
    "coherency",
    "field-coupling"
   ],
   "severity": "high",
   "wrong": "Treating any number in contribute({coherence}) as a coherency reading.",
   "truth": "wire-field-couplings.js auto-wired 67 sites from a name-shape list; 41 contributed confidence, matchScore, density, a running total or a zlib savings ratio. The [0,1] clamp made each type-correct so nothing failed. Removed 2026-08-01; the wirer is disabled.",
   "tell": "A contribution whose expression names something other than coherency, or resolves to a local that was assigned from one.",
   "correct": "node scripts/audit-field-contributions.js before trusting the field. A coherency must originate from the Void compressor."
  },
  {
   "match": [
    "void-bridge",
    "substrateCoherence",
    "_substrateCoherenceScore"
   ],
   "severity": "high",
   "wrong": "Reading void-bridge.js's substrateCoherence as a Void compressor measurement because of the file and field names.",
   "truth": "It computes a JS byte-frequency Shannon entropy and inverts it. It never calls the compressor — its own comment says '(In full implementation, this calls the Python compressor)'.",
   "tell": "The name says Void; the body has no HTTP call, no subprocess, no import of the compressor.",
   "correct": "Use compressor_service /compress_signal avg_coherence for a real reading."
  },
  {
   "match": [
    "goggle-web",
    "--fast",
    "browse",
    "web"
   ],
   "severity": "medium",
   "wrong": "Assuming a page read at --fast speed went through the Void compressor.",
   "truth": "--fast substituted zlib and reported coherency null, then contributed resonance.meanTop5 or a literal 0.5 to the field under the name coherency. Removed 2026-08-01; the reader now refuses rather than falling back.",
   "tell": "method 'fast(zlib)' in a reading, or coherency null.",
   "correct": "Readings now carry via:'void:compress_signal'. Anything without it did not pass through the compressor."
  },
  {
   "match": [
    "CANONICAL",
    "version",
    "v3",
    "v4",
    "v5",
    "entry point"
   ],
   "severity": "medium",
   "wrong": "Reading whichever version file you opened first and describing it as current.",
   "truth": "CANONICAL.json names the entry point per family. void_compressor v5 is canonical; v3/v4 are load-bearing-internal — they still execute under v5, so they are neither dead nor current. compressor_service.py was itself instantiating v4 while v5 was canonical.",
   "tell": "Describing an API from a file whose role you did not check.",
   "correct": "python3 scripts/build_canonical_manifest.py, or read CANONICAL.json, before quoting any versioned API."
  },
  {
   "match": [
    "benchmark",
    "baseline",
    "gzip",
    "compression ratio"
   ],
   "severity": "medium",
   "wrong": "Benchmarking against gzip alone and reporting a win.",
   "truth": "gzip is DEFLATE with a 32 KB window from 1992. The field is xz -9e, zstd -19, brotli -q 11, bzip2 -9, PPMd, and zstd with a trained dictionary. On signal Void beats 5 of 6; on text it ties gzip and loses to everything.",
   "tell": "A comparison table with one baseline in it.",
   "correct": "scripts/benchmark_signal.py for waveforms, scripts/benchmark_baselines.py for text. Train any dictionary on a disjoint half — training on the scored bytes inflated results 1.7x-2.1x."
  },
  {
   "match": [
    "regex",
    "exec",
    "taint",
    "security",
    "shell injection"
   ],
   "severity": "low",
   "wrong": "Flagging X.exec(s) as shell injection because the method is named exec.",
   "truth": "looksLikeRegex decided by NAME suffix (_RE, PATTERN, Matcher), so regexes named for meaning — SEAL, METRIC, CAVEAT — were reported as injection. Fixed by reading the actual binding.",
   "tell": "A security finding on a file with no child_process import.",
   "correct": "collectRegexIdents in src/audit/taint.js reads which identifiers are bound to a regex."
  },
  {
   "match": [
    "delta",
    "diff",
    "uint8",
    "quantise",
    "quantize"
   ],
   "severity": "low",
   "wrong": "First-differencing uint8 samples into int8, or prepending a[0].",
   "truth": "uint8 steps span -255..255 and int8 holds -128..127, so large steps are destroyed. prepend=a[:1] zeroes the first difference and loses a[0]. Both produced a lossy 'baseline' that was the SMALLEST number in the table and would have been crowned had round-trips not been verified.",
   "tell": "A compression result that does not decode back to the input.",
   "correct": "Modular uint8 arithmetic, prepend 0, and verify the round-trip on every row."
  },
  {
   "match": [
    "normalis",
    "normaliz",
    "window",
    "goggle-web",
    "512"
   ],
   "severity": "medium",
   "wrong": "Reading a low compression ratio on signal as a weakness of the compressor.",
   "truth": "goggle-web.js min-max normalises EACH 512-window independently to the full 0-255 range, pushing every window toward maximum entropy at 8 bits. Much of the smoothness the fractal matcher needs is destroyed before the compressor sees it. Global normalisation is untested.",
   "tell": "Every ratio across every method sitting near 1.0-1.6x.",
   "correct": "State the quantisation with any ratio taken through this path."
  },
  {
   "match": [
    "seriesCoherence",
    "fractal",
    "toFractalWaveform",
    "composed_v1",
    "composed_v2",
    "29-D",
    "feature vector",
    "ingest coherency"
   ],
   "severity": "high",
   "wrong": "Applying a SERIES statistic (seriesCoherence, autocorrelation, trend fit, FFT, smoothing) to a fractal/composed vector because it is an array of numbers.",
   "truth": "The 29-D fractal vector is not a waveform. Its slots are NAMED HETEROGENEOUS FEATURES in the order fractal-waveform.js lists them: charge, valence, mass, spin, phase, reactivity, electronegativity, group, period, safety, alignment, intention, depth_l0..l4, keywords, identifiers, strings, numbers, operators, branches, loops, functions, returns, errors, comments, structurality. Slot 7 (group) is the constant 11 for every input. Adjacent slots are unrelated quantities, so lag-1 autocorrelation and a trend fit over slot index measure the encoder authoring order, not the artifact. composed_v1/v2 are concatenations of such vectors and are worse.",
   "tell": "The reading is near-constant across wildly different files (0.139-0.190 for api.py, securityScanner, isEmpty, buggy, computeSpin), and permuting the slot order — the same relabeling for every file — moves it by more than its entire spread across files. Measured: r(reading, slot-permuted reading) = 0.036; r(reading, compressor-on-the-same-file-bytes) = -0.025.",
   "correct": "Take coherency off the Void compressor reading the artifact BYTES: POST /compress_signal with Array.from(Buffer.from(content)) and use avg_coherence. seriesCoherence is valid and accurate on a genuine time series — against synthetic controls it tracks the compressor within ~0.1 from pure sine 1.000 down to white noise 0.152 — so the function is not the bug; pointing it at a feature vector is."
  },
  {
   "match": [
    "HARM_PATTERNS",
    "covenant-harm",
    "stripNonExecutableContent",
    "covenantCheck",
    "harm pattern",
    "strippedCode"
   ],
   "severity": "medium",
   "wrong": "Assuming a harm pattern that runs on strippedCode can never match text inside a string literal, and writing a short generic pattern on that assumption.",
   "truth": "stripNonExecutableContent does not reliably remove string BODIES. On src/audit/explain.js it collapsed delimiters and commas but left the content: a literal array of example lines came through as \"''function run(req) {''  eval(req.body.code);''}''\". Long/specific patterns (SQL keywords plus concatenation) survive this because the surviving prose rarely contains them; short generic shapes like eval(<ident>) or new Function(<ident>) match example code and documentation immediately.",
   "tell": "A new harm pattern fires on audit/explain, docs, test fixtures or the rule-description files rather than on real code — the classic covenant-mismatch shape where comments describing a rule trigger the rule they describe.",
   "correct": "Scan the whole src tree before trusting a new pattern (covenantCheck over every file, count what newly blocks). Files that legitimately contain counterexamples carry @oracle-pattern-definitions and must be checked with {trusted:true} for the exemption to apply. Prefer patterns anchored on something prose will not contain, and do not rely on the stripper alone."
  },
  {
   "match": [
    "defect-resonance",
    "resonance:",
    "shape channel",
    "META-DEBUG",
    "taught by seed",
    "languageBound",
    "unwrap-chain"
   ],
   "severity": "medium",
   "wrong": "Reading a shape-channel resonance finding as a defect claim without checking whether the signature that matched was taught in the same language as the file.",
   "truth": "defect-resonance transfers defect shapes across languages BY DESIGN. But a seed whose meaning is a language CONSTRUCT does not transfer. unwrap-chain-panic describes Rust .unwrap() panicking; its form is a short chain of method calls with unchecked results, which is most of JavaScript. It matched express lib/response.js and lib/application.js at min-depth 0.926-0.936 and reported HIGH three times, every one a false positive. eval-of-input and sql-by-concat carry per-language variants precisely because those concepts are universal.",
   "tell": "A HIGH finding whose message ends in \"taught by seed, LANG\" where LANG is not the language of the file being scanned.",
   "correct": "Language-bound seeds carry languageBound:true and are skipped on a language mismatch (src/debug/defect-resonance.js). Verified both ways: the same Rust source fires unwrap-chain-panic scanned as rust, silent scanned as javascript. When adding a seed, ask whether it names a concept every language has, or a construct one language has."
  },
  {
   "match": [
    "composedAtDepth",
    "cosine(",
    "flowCosines",
    "composedCosine",
    "toFractalWaveform",
    "coherencyOf",
    "node -e",
    "pattern_index_fractal"
   ],
   "severity": "high",
   "wrong": "Verifying a claim about the instrument by writing your own computation over its parts — a scratch cosine, a hand-rolled vector extractor, a direct read of pattern_index_fractal.json, a numpy brute force over FractalRetroSearch.",
   "truth": "The pipeline IS the measuring apparatus, not the thing being measured. A reading carries entanglement + substrate + field coupling + depth flow + covenant; strip any of them and the number is a different quantity, not a degraded version of the same one. Measured instances: seriesCoherence on the 29-D feature vector read 0.19-0.23 where the compressor read 0.186 on the same files — right magnitude, r = -0.025, unrelated quantity. A hand-rolled cosine over composedAtDepth produced a \"noise outscores signal\" claim that had to be retracted; the same question asked through --do call produced the real defect (flowCosines caps at 116, so L5-L8 never reach any reading).",
   "tell": "You are about to report a number you computed yourself as though it described the instrument. Or: the fast path took 2s where --do call would take 60s. Or: you are describing your own method in the report — \"I computed cosine over ...\" — which is the bypass admitting itself.",
   "correct": "Ask the instrument. goggles --do call <path>#<fn> runs any exported function with JSON args; --do contracts, --do field, --do drift, --do audit cover the rest. If no verb reaches what you need, that is a MISSING VERB to report, not a licence to bypass. The absence of a route is itself a finding."
  },
  {
   "match": [
    "flowCosines",
    "coherencyFlow",
    "pairwiseFlow",
    "d1, d2, d3, d4",
    "[d1,d2,d3,d4]",
    "depth flow",
    "flowValues",
    "deepestFlow"
   ],
   "severity": "high",
   "wrong": "Destructuring a depth-flow into d1..d4, or indexing [3] as \"the deepest\".",
   "truth": "flowCosines returns ONE reading PER ACTIVE LAYER — eight today, checkpoints [29,58,87,116,145,174,203,232] — and the count changes whenever a layer activates. Four names silently mean 116 of 232 dimensions, discarding L5-redundancy, L6-content, L7-dimensional and L8-dynamical: exactly the layers that separate look-alikes. Measured when coherency-mapper.js was still doing it: fixing the three sites moved Void duplicate pairs 35->30 (format-echo 9->4) and orphans 36->43, hub orphans 77->91, on identical substrate state.",
   "tell": "A literal 116, a CHECK array ending at 116, `const [d1,d2,d3,d4] =`, or classifyFlow({d1,d2,d3,d4}). Also: a private _flow/_cosine helper in any module that is not decoder-stack.",
   "correct": "Call decoder-stack.flowCosines and keep the array. deepestFlow(flow) for the deepest, Math.min(...flow) for the floor, classifyFlow(flow) for the shape — all handle any length. Never name the elements."
  },
  {
   "match": [
    "DEFAULT_THRESHOLD",
    "threshold",
    "calibrat",
    "ENCODE_DEPTH",
    "defect-resonance",
    "min-depth",
    "floor"
   ],
   "severity": "high",
   "wrong": "Keeping a calibrated threshold after changing the width it was calibrated at, or setting one at the midpoint of a gap without checking the gap is positive.",
   "truth": "A min-depth floor is only meaningful at the width it was measured at — adding checkpoints can only lower a minimum. And the gap can be INVERTED: measured on defect-resonance at the canonical width over 1,510 blocks of real ecosystem source, max clean read 0.9486 (_compose in fractal-index.js, a correctly-bounded loop) while a genuine off-by-one variant read 0.7703. Gap -0.178. Swept at depths 4,5,6,7,8: identical edges every time, because min(...flow) lands on the 29/58/87-D checkpoints in 95% of readings. At the old 0.91 the channel fired HIGH on three live core files, two of them template strings containing no SQL.",
   "tell": "You are about to publish a threshold without printing max-clean and min-defect beside it. Or the recommendation is a midpoint and you never checked the sign of the gap.",
   "correct": "Run scripts/calibrate-defect-resonance.js. It measures both edges through the channel's own scan() in dryRun and refuses to suggest a midpoint when the gap is inverted. When they overlap, follow the module's stated policy — prefer misses — set the floor above the clean edge, and PUBLISH the recall cost."
  },
  {
   "match": [
    "domains",
    "domain",
    "label",
    "pattern_uri_index",
    "label_all_patterns",
    "coh://",
    "source_stems",
    "taxonomy",
    "mislabel"
   ],
   "severity": "high",
   "wrong": "Reading the `domain` string on a pattern as a subject label, and calling a mismatch a labelling defect.",
   "truth": "label_all_patterns.py assigns it by `corr = W @ D.T; argmax(|corr|)` against the resonance detector's domain matrix — a grouping by STRUCTURE, with a human-readable string attached afterwards so people can talk about it. The instrument never reads it back; it is not an input to anything. A cryptocurrency series carrying the string `physics` is reporting waveform shape, not a bookkeeping error. A whole 'the labels are unreliable' finding was committed against this and had to be retracted (Void d47ea54 -> dba6a8e).",
   "tell": "You are about to describe a stored string as right or wrong, or to 'fix the labels'. Ask instead what produced it — if the answer is an argmax over correlations, the category does not apply.",
   "correct": "Treat the strings as read-out only. To ask what actually groups with what, use `goggles --do cluster`, which correlates patterns directly with no grouping."
  },
  {
   "match": [
    "median",
    "mean",
    "average",
    "avg",
    "aggregate",
    "summary",
    "p50",
    "distribution"
   ],
   "severity": "high",
   "wrong": "Reporting a median, mean or range as though it were the reading.",
   "truth": "Coherency is produced once by the compressor per artifact and is time-independent. An aggregate over a sample is a different quantity, and it silently changes what was measured: a median once reported here described 7% of the substrate and was presented as the whole. Nine averaging sites were removed for this reason; the two that survive (DomainSignature.mean_waveform, and coherence_index = mean|off-diagonal| at resonance_detector.py:1140) average WAVEFORMS and CORRELATIONS, not coherency readings — say which when quoting them.",
   "tell": "Your sentence contains 'median', 'average', 'roughly' or 'typically' where a number belongs.",
   "correct": "`goggles --do state` returns the raw readings as the compressor produced them, unaggregated. Quote those, and say how many of the total they are."
  },
  {
   "match": [
    "lift",
    "chance",
    "baseline",
    "control",
    "significance",
    "x lift",
    "over chance",
    "nearest neighbour",
    "nearest neighbor"
   ],
   "severity": "high",
   "wrong": "Scoring a result against a label, name or string that the same pipeline wrote.",
   "truth": "Circularity reads exactly like signal. A 3.09x 'lift over chance' was reported here from nearest-neighbour agreement on `domains` — a field that resonance itself had populated by argmax correlation. The number was real arithmetic on a question that could not have answered otherwise, and it had to be retracted (Void dba6a8e: 'should not be cited').",
   "tell": "Before reporting a lift, trace who wrote the column you scored against. If the instrument wrote it, the test is measuring the instrument agreeing with itself.",
   "correct": "Score against something the pipeline never touched, or state plainly that the comparison is internal consistency, not validation."
  },
  {
   "match": [
    "composed",
    "composed_v1",
    "composed_v2",
    "composed_v5",
    "redecode",
    "re-decode",
    "no-op",
    "unchanged",
    "index",
    "ceiling"
   ],
   "severity": "medium",
   "wrong": "Concluding a rebuild was a no-op, or that a store has a ceiling, from whichever index file you opened.",
   "truth": "Both were reported here and both were wrong. `--do redecode` wrote a `composed` field while void-map and substrate-state still read `composed_v2 || composed_v1`, so a completed re-decode read as no change. Separately '8,871 is the ceiling' and '44,906 entries unreachable' were both stated while 42,712 canonical vectors already sat in composed_v5_index.json.",
   "tell": "You are about to report that nothing changed, or that a limit exists, based on one file and one field name.",
   "correct": "Check which field the WRITER wrote and which the READER reads — they drift apart independently. Counts come from `goggles --do state`, not from whichever index you opened first."
  },
  {
   "match": [
    "traps.json",
    ".remembrance",
    "goggles-learning",
    "defect-signatures",
    "learned",
    "gitignore",
    "lessons"
   ],
   "severity": "medium",
   "wrong": "Writing a lesson into .remembrance/ and treating it as kept.",
   "truth": "`.remembrance/` is gitignored in full (.gitignore:2). Everything the system has learned — traps.json, defect-signatures.json, goggles-learning.json — lives only on the host that learned it. On a fresh container it is gone, and the loop restarts from seeds. defect-resonance already anticipates this and tries goggles-memory.restore() from the chain before seeding; traps.json had no such path and no tracked source at all.",
   "tell": "You just added knowledge to a path under .remembrance/ and did not add it anywhere tracked.",
   "correct": "Add the durable copy to seeds/traps.seed.json, which IS tracked, and let the local store merge on top."
  },
  {
   "match": [
    "separation",
    "no separation",
    "threshold",
    "AUC",
    "one-class",
    "two-class",
    "margin",
    "calibrate",
    "gap"
   ],
   "severity": "high",
   "wrong": "Declaring 'no separation' from a one-class absolute threshold when the comparative question was available.",
   "truth": "The previous calibration asked 'how strongly does this resonate with a defect shape' and gated on an absolute floor — and working code answered ~0.95 because code resembles code. Asked comparatively (margin = defect resonance - working-code resonance, both classes through the channel's own scan), the same eval set gives a usable rule the absolute floor never could: margin>0 catches 5 of 7 buggy variants at 11/307 (3.6%) clean false positives, where the 0.955 floor caught 2 of 7. The 'inverted gap' was an edge statement about two extreme blocks; ranking across the full distributions was informative all along (AUC ~0.98 both ways). The conclusion 'the channel cannot tell these apart' was itself drawn from one number.",
   "tell": "Your verdict cites only max-clean and min-defect. Or you scored similarity to one class without asking what the alternatives were.",
   "correct": "Ask the comparative question THROUGH THE EXISTING SURFACE — scan() in dryRun against the defect library, then a working-code library taught via teach(), margin = defect - working. A standalone script for this was added then REMOVED (the substrate already had every part); do not re-add one."
  },
  {
   "match": [
    "specimen",
    "seed",
    "SEEDS",
    "fixture",
    "covenant",
    "harm pattern",
    "oracle-pattern-definitions",
    "oracle-infrastructure",
    "COVENANT BROKEN"
   ],
   "severity": "medium",
   "wrong": "Rewriting a defect SPECIMEN to get past the covenant's harm scanner.",
   "truth": "A file that defines bad patterns must be allowed to contain them. The covenant flagged execSync('ls ' + dir) inside a template literal in validate-defect-seeds.js — accurate about the text, wrong about the file, because nothing executes a fixture. The established route is the @oracle-pattern-definitions annotation, carried by covenant-harm.js, covenant-deep-security.js and reflection-scorers.js for exactly this. It is not a bypass: covenant.js:81-83 only honours it when the CALLER passes {trusted:true}, so submitted code cannot self-declare. The covenant scanner even splits its own require('child'+'_process') to avoid self-matching.",
   "tell": "You are about to obfuscate a string, split a literal, or soften a specimen so a gate stops complaining. That edits the evidence to fit the instrument.",
   "correct": "Declare the file for what it is with @oracle-pattern-definitions and say why in the header. If a specimen is ever EXECUTED rather than encoded, the annotation is a lie — delete the specimen instead."
  },
  {
   "match": [
    "updateVoterReputation",
    "unwired",
    "never called",
    "no caller",
    "dead export",
    "wire",
    "capability"
   ],
   "severity": "high",
   "wrong": "Assuming a capability is wired because it exists — complete implementation, docs, even a test comment claiming coverage.",
   "truth": "sqlite updateVoterReputation() sat fully implemented (+0.05/-0.03 reputation, capped [0.1,3.0]) with ZERO callers; the test asserting it hid behind if(!patternId) return and passed vacuously for its whole life. Reputation could never move. Found 2026-08-07 while clearing the perpetual red test.",
   "tell": "grep for the function name returns only its definition (and maybe a comment). A test guard like if(!fixture) return that can silently skip every assertion. A feature whose observable output never changes from its default.",
   "correct": "Count callers before trusting any capability: rg \"\\bNAME\\s*\\(\" outside the defining file. Zero src callers = unwired — wire it through the real consumer path or delete it. Never let a test early-return on missing fixtures; fail loudly instead."
  },
  {
   "match": [
    "audit-field-contributions",
    "contribute site",
    "write surface",
    "census",
    "matcher",
    "field source"
   ],
   "severity": "high",
   "wrong": "Trusting a census because it runs — the field-contribution audit was believed to see every contribute({coherence}) site in src/.",
   "truth": "Its matcher required the literal key order cost:, coherence:, source: on ONE line. Any hand-written site with a different key order, or a call formatted across lines, was invisible. Found 2026-08-09 while building the field-source ratchet: the widened order-independent matcher revealed 68 sites where the audit reported 29 — 39 write sites had never been audited, and 22 of the 68 carry non-coherency quantities awaiting adjudication.",
   "tell": "A census whose regex encodes a spelling (argument order, one-line formatting, a specific quote style). Verifying a gate by only feeding it inputs shaped like the ones it already counts.",
   "correct": "Adversarially test every census with a syntactically-valid variant it should catch but formatted differently (reordered keys, multi-line, renamed receiver). If the count does not move, the census has a spelling requirement — widen extraction (balanced parens, order-independent fields), never narrow the input."
  },
  {
   "match": [
    "cycle-ratchet",
    "require cycle",
    "lexical",
    "load-time",
    "lazy require",
    "SCC",
    "62-file"
   ],
   "severity": "high",
   "wrong": "Measuring lexical require mentions and calling them cycles — then planning 8 lazy-require cuts to untangle a 62-file knot.",
   "truth": "A require inside a function body or expression-bodied arrow runs at CALL time and cannot deadlock module init. Position-aware measurement (tokenizer, fn-body + expression-arrow contexts) showed the load-time require graph of src/ was ALREADY ACYCLIC — every cycle-participating edge was already lazy, largely thanks to prior decompositions and report-lazy. The planned cuts would have converted already-lazy edges while the lexical census read 62 unchanged: execution theater against a misread instrument. Found 2026-08-09 while executing the cut plan with byte-level precision.",
   "tell": "A remediation plan whose success metric is read from an instrument that cannot distinguish the cure from the disease (lazy vs top-level require, demonstration vs instance, mention vs call). The first cut appearing to change nothing.",
   "correct": "Before executing a graph-surgery plan, measure the graph the failure mode actually lives on (load order for init deadlock; lexical for coupling). The ratchet now censuses BOTH: load-time gated at zero, lexical shrink-only through real decomposition."
  },
  {
   "match": [
    "exemption shed",
    "@oracle-infrastructure",
    "fractalAudit",
    "covenantCheck",
    "SQL identifier",
    "quantum-field",
    "load-bearing exemption"
   ],
   "severity": "high",
   "wrong": "Deciding an @oracle-infrastructure exemption is sheddable because fractalAudit (byte+atomic) reads clean once the blocking findings are resolved.",
   "truth": "The exemption tag relieves a file from MORE than one scanner. quantum-field.js reached byte:0 atomic:0 after atomic annotation, but its tag was load-bearing for the COVENANT SQL-identifier rule (principle 11) — its  DDL interpolations are identifiers SQL cannot parameterise, and the file header says so. Removing the tag left fractalAudit green but covenantCheck BROKEN; the pre-commit gate would have blocked it. Found 2026-08-09 shedding the src exemption tranche — a file-modification note surfaced the header just in time to check covenantCheck before trusting the shed.",
   "tell": "A file whose header EXPLAINS why it is exempt (SQL identifiers, dense code, a scoped security-rule exemption) — that prose is the load-bearing reason, and it names a scanner fractalAudit does not run.",
   "correct": "Before shedding any exemption, run BOTH covenantCheck (covenant.js — SQL/harm/injection, principle 11) AND fractalAudit (covenant-fractal.js — byte mutations + atomic properties). An exemption is only sheddable when both read clean. Read the file header first: if it argues for its own exemption, that argument is usually the second gate."
  },
  {
   "match": [
    "silent-catch",
    "swallowed",
    "rg count",
    "template literal",
    "tokenizer",
    "false alarm",
    "backtick parity",
    "census blind spot"
   ],
   "severity": "high",
   "wrong": "Concluding a census is blind — and alarming that a gate lied — because a raw rg count of try/catch clauses exceeds the tokenizer count, then trusting the rg number.",
   "truth": "Files that GENERATE code (hooks.js emits pre-commit/post-commit/pre-push shell+JS via template literals; explain.js/debug-seeds.js hold code examples) contain try/catch as STRING content inside template literals. rg counts that string content; the substrate tokenizer correctly excludes it. hooks.js: rg saw 32 catch clauses, the tokenizer saw the 2 real ones — and was RIGHT. The 30 others were inside three script-generating templates (lines 39-336, 343-573, 583-643). Verified 2026-08-09: token SPANS place every extra catch inside a string/template/comment; the real-code regions have 2 catches, both handled; 0 real swallows. The 462 to 0 claim held. The re-check that called it a lie was itself the error — worsened by a backtick-parity heuristic that shell command-substitution backticks inside the generated scripts defeated.",
   "tell": "A raw-text (grep/rg) structural count that disagrees with the parser, on a file that builds strings of code. A parity/brace/quote heuristic applied to a file containing generated shell or JS. Alarm rising faster than verification.",
   "correct": "Classify each raw match by the parser token SPAN it falls in (string/template/comment vs real code) before trusting it — token spans are reliable even when the tokenizer.line labels drift. scripts/silent-catch-ratchet.js --verify does exactly this and shows its work. Never trust raw grep over token spans for structural claims about code that generates code; never trust backtick/quote parity on a file that emits shell."
  },
  {
   "match": [
    "goggles",
    "src/tools/goggles.js",
    "run.mjs",
    "--do",
    "goggle the file"
   ],
   "severity": "high",
   "wrong": "Running `node src/tools/goggles.js <file>` and reporting the result as a goggles read.",
   "truth": "src/tools/goggles.js is the IMPLEMENTATION. The surface is .claude/skills/goggles/run.mjs, and it is the only path carrying --do: ratchets, call, field, drift, harvest, state, void, size, exemptions and 17 more. Invoked directly the tool still prints FOCUS/META/MACRO, so the read looks right while every routed operation is unreachable. Measured 2026-08-12: an entire working session of direct invocation, during which the gates were run by hand and --do was never available at all.",
   "tell": "`--do <verb>` fails with `cannot read .../--do: ENOENT`. That is the tool saying it is not the surface.",
   "correct": "node .claude/skills/goggles/run.mjs <file> | --map | --diff | --do <verb>. Read SKILL.md and the verb list ONCE at the start of work in a repo — not after the third bypass. A plausible-looking read from the wrong entry point never announces itself."
  },
  {
   "match": [
    "/tmp",
    "scratchpad",
    "probe",
    "harness",
    "hand-rolled",
    "census",
    "measurement script"
   ],
   "severity": "high",
   "wrong": "Writing a measurement to a scratch script and running it, because finding the verb that already computes it takes longer than writing one.",
   "truth": "Generating code is the cheapest available action and discovery is not, so the reflex is to construct rather than look — and the constructed instrument is reliably the WORSE one. On 2026-08-12 every hand-rolled probe either duplicated an existing surface or produced a false positive the substrate then corrected: a grep timer-leak finding died on covenant-harm.js (regex BUILDERS, not calls), a 73-site process.exit count fell to 17 under token spans, and a 20.9% atomic-drift figure was wrong because `function f(opts = {}) {` had its parameter default scanned as the body. Sparse real readings beat dense invented ones.",
   "tell": "You are about to run `node /tmp/....js` and the script requires — or merely names in a string — an ecosystem module.",
   "correct": "goggles --do <verb>, or --do call <repo>/<path>#<fn> [jsonArg …]. If no verb or export reaches it, that is a MISSING VERB to report, not a bypass. A scratch CODEMOD over fs/path is honest work; a scratch MEASUREMENT is the bypass."
  },
  {
   "match": [
    ".remembrance",
    "diagnostics",
    "cathedral-latest",
    "baseline",
    "stale",
    "gitignored"
   ],
   "severity": "high",
   "wrong": "Reading a file under .remembrance/ as a live measurement and reporting the repo state from it.",
   "truth": ".remembrance/ is gitignored in full, so its contents are whatever the last run on THIS host left behind and can predate the working tree by any amount. On 2026-08-12 the covenant ratchet was reported failing — `main is red on its own gate, high 21 vs seed 16` — from a cathedral-latest.json that predated the very merge which fixed those five findings. A fresh scan read 16/172/11 and the gate held. The claim was published before the artifact age was ever checked.",
   "tell": "A verdict traces back to a file under .remembrance/ and its generatedAt has not been compared against the working tree.",
   "correct": "Regenerate the diagnostic, or compare generatedAt to the last commit touching the scanned files, BEFORE quoting any number out of .remembrance/. A derived artifact is evidence of a past run, never of the present state."
  },
  {
   "match": [
    "structureStrong",
    "measurableOnly",
    "weak / novel structure",
    "verdict band",
    "coherency threshold",
    "WELL-FORMED"
   ],
   "severity": "high",
   "wrong": "Quoting a coherency verdict (`weak / novel structure`) without checking which quantity its bands were calibrated against.",
   "truth": "The bands were 0.93/0.80/0.70, commented `(measurableOnly scale)` — the RETIRED syntax score whose median was ~0.83. The live reading is the Void compressor self-compression scalar: pure repetition (`AB` x2048) reads 1.0000, a 16-byte cycle 0.9960, high-entropy random 0.0872, and real source code 0.098-0.278. Nothing clears 0.70 — 0 of 28 sampled files did — so every file in the repo printed the same bottom verdict for a full session and the number carried no information. Coherency is how well a signal compresses INTO ITSELF; low on source code is the CORRECT answer, not a complaint about the code.",
   "tell": "Every file returns the same verdict, or a verdict band and its reading disagree by an order of magnitude.",
   "correct": "Anchor against the instrument before quoting a verdict. Bands live in living-remembrance gogglesParams as coherencyRepeating/Mixed/Typical. Related: MACRO`s WELL-FORMED / ORPHAN / DUPLICATE flags come from SIBLING SIMILARITY, not coherency — 1,335 files carry WELL-FORMED with scoredCount 0, i.e. no coherency measured at all."
  },
  {
   "match": [
    "experiments/",
    "random.random",
    "np.random",
    "randn",
    "def norm",
    "min-max",
    "minmax",
    "normalize(",
    "noise domain",
    "surrogate",
    "resonance_connect",
    "whitened_resonance",
    "gen_surrogates",
    "combined_score",
    "what the instrument sees",
    "encode_batch",
    "_compute_signature",
    "ResonanceDetector"
   ],
   "severity": "high",
   "wrong": "Answering a MEASUREMENT request by BUILDING: writing a new script under experiments/ (or a *.cjs) that imports the instrument's parts (ResonanceDetector, composedAtDepth, encode_batch, _compute_signature), wires its own glue (an invented min-max / norm() step, a hand-chosen depth), and INJECTS self-generated random.random()/np.random data as a 'noise' domain — then reporting the stdout in the instrument's vocabulary ('what the instrument sees', 'resonance', 'coherency', combined_score) as if the pipeline produced it.",
   "truth": "Importing an instrument function is NOT running the instrument's pipeline, and a repo-resident experiment script is not more faithful than a /tmp one (trap 29) — both are PROXIES beside the pipeline, and the standing rule throws out any result a proxy produced. Every glue choice silently changes the quantity: on 2026-08-23 an invented per-window min-max shifted a DC term that then saturated the FFT-magnitude cosine, so ONE component (spectral overlap 0.97-0.99, ~identical even against real noise) dominated every pair — the 'one signal dominating means you ran it wrong' tell. And random.random() 'noise' is not a control the user asked for; it is fabricated data placed inside a measurement about real brain/heart/attention signals.",
   "tell": "You just created a file under experiments/ or a *.cjs; it contains random.random()/np.random, a norm()/min-max/normalize step you wrote, or an import of composedAtDepth|ResonanceDetector|encode_batch; and you are about to describe its output as 'what the instrument saw'. The session shape was 'I built X, then ran X' instead of 'I called the goggles/service'. A single component reads ~identical across every pair (noise included) — the pipeline was fed data through a normalization it never applies.",
   "correct": "Do not build. Call the one surface on the substrate the instrument already holds: node .claude/skills/goggles/run.mjs --do <verb> (resonance, field, audit, drift, call ...) or POST /compress_signal on compressor_service.py — and report ONLY what it returned. Never inject generated/random data into a measurement. Never invent a normalization the pipeline does not already apply. If no verb reaches the measurement, that is a MISSING VERB to add to the goggles (see traps 13, 29), not a licence to hand-assemble a script beside the pipeline. Never dress hand-built output in the instrument's vocabulary."
  },
  {
   "match": [
    "_rebuild_pattern_matrix",
    "pattern_matrix",
    "dimension partition",
    "legacy256",
    "matmul",
    "compress_signal",
    "void_compressor"
   ],
   "severity": "high",
   "wrong": "Choosing the compressor's L1 search matrix by the MOST COMMON vector width in pattern_library.",
   "truth": "On a clean clone pattern_store.legacy256.npz is gitignored and absent, so the 232-D fractal store (45k rows) outnumbers the signal patterns; the matrix came out 232 wide while the blend search resampled every chunk to 256, and every compress() died in the matmul. compressor_service answered HTTP 500 on /compress_signal and every goggle, harvest, seal and coin in the ecosystem read NULL. The instrument was down on every fresh container and nothing said so.",
   "tell": "`ValueError: matmul ... size 256 is different from 232` in compressor_service.log; `--map` reports 0 files audited and 'answered without a usable avg_coherence'; every FOCUS reads NULL.",
   "correct": "The search space is chosen by LAW (the dimension the encoder emits), never by census. One encoder (ECOSYSTEM §7): the 232-D fractal decoder. Never depend on a gitignored store for the instrument to work at all."
  },
  {
   "match": [
    "coherencyScore",
    "covenant-gate",
    "CovenantGate",
    "Coherency too low",
    "minimum 0.8",
    "git-history-coin",
    "publish("
   ],
   "severity": "high",
   "wrong": "Putting the Void compressor's byte reading into the Covenant Gate's `coherencyScore.total` slot and calling the refusal 'the gate holding its bar'.",
   "truth": "That slot is the oracle structural score (computeCoherencyScore(code).total) — the quantity the 0.8 bar was calibrated on and the one Publisher records as its second opinion. On the compressor's scale nothing but a self-repeating signal clears 0.8 (source code reads 0.08–0.28), so the bar was never set against it. The coin was refused at 0.0896 for a category mismatch, and ALSO because the script never set testCode — the gate was structurally unpassable regardless of any number.",
   "tell": "A gate refusal whose number sits in the compressor's source-code band (≈0.1) against a threshold ≥0.8; a payload with no testCode.",
   "correct": "Each quantity under its own name: the gate slot carries the score it was calibrated on; the compressor's reading rides beside it as waveformCoherency with its void-seal mint. Run the pattern's real test and hand it to the gate as testCode. Never rename one reading into another's slot."
  },
  {
   "match": [
    "--do read",
    "--do covenant",
    "--do call",
    "run.mjs --do",
    "goggles --do",
    "execFileSync(cmd, cmdArgs, { cwd"
   ],
   "severity": "high",
   "wrong": "Passing a RELATIVE path to a goggles --do verb (read, covenant, call) from the repo you are standing in.",
   "truth": "Every --do verb runs its script with cwd set to the toolkit (or Void), so a relative path resolves THERE. `--do covenant src/index.js` from the blockchain repo audited the TOOLKIT's src/index.js and reported it CLEAN — a verdict on the wrong file, indistinguishable from a verdict on yours. `--do read data/x.json` reported 'no such file'; `--do call ... \"$(cat missing)\"` measured an EMPTY STRING and printed a full breakdown for it.",
   "tell": "A --do verdict that arrives suspiciously fast or clean, a 'no such file' from a path that exists, a computeCoherencyScore breakdown with functions: 0 / lines: 0.",
   "correct": "Absolute paths, always. Check the arg file exists before `$(cat ...)`. When a verb resolves paths itself, that is a missing feature to report, not a reason to trust the output."
  },
  {
   "match": [
    "new Publisher(",
    "Publisher(",
    "persistPath: '.remembrance/ledger.json'",
    "ledger.json",
    "recordEvent("
   ],
   "severity": "medium",
   "wrong": "Publishing to the ledger with a default Publisher and believing the block is on the chain.",
   "truth": "The default Publisher starts a FRESH GENESIS at the cwd-relative runtime ledger (.remembrance/ledger.json, gitignored) — it never loads an existing chain, so the first recordEvent overwrites whatever was there and the block evaporates with the container. The committed chain is data/ledger.json; nothing reached it.",
   "tell": "`node src/cli.js publications` shows demo data; data/ledger.json unchanged after a 'successful' publish; git status clean.",
   "correct": "new Publisher({ ledgerPath: 'data/ledger.json' }) — loads, hash-verifies and APPENDS to the committed chain (PatternLedger.load(path, { persistPath })). Then commit data/ledger.json."
  },
  {
   "match": [
    ".remembrance/",
    "pattern_index_fractal.json",
    "legacy256",
    "gitignore",
    "clean clone",
    "fresh container",
    "data-plane",
    "git-history-coin"
   ],
   "severity": "high",
   "wrong": "Assuming the substrate index, the legacy signal store, the seal key, or any lesson under .remembrance/ exists on the host you are on.",
   "truth": "All of them are gitignored. On this clone pattern_index_fractal.json, pattern_store.legacy256.npz, .substrate_seal.key and .remembrance/substrate-state.json were all absent; `--map` said 'substrate has no vectors for this repo' and the coin's seal could not be signature-verified. The recovery coin's data plane (data/data-plane/substrate.json.gz, sha-anchored) IS the way back for the index; nothing recovers legacy256 and nothing should — it is retired.",
   "tell": "'substrate has no vectors', a seal with state_id 'unknown', a compressor library_size far from the store's row count.",
   "correct": "Verify the coin (tests/git-history-coin.test.js), then gunzip its data plane back to the `source` paths. Treat every .remembrance/ artifact as evidence of a past run on some host, never as present state."
  },
  {
   "match": [
    "BUILT",
    "tier1_validator",
    "fractal_token",
    "tier1_calibrate",
    "TIER1_RECOMPUTATION_GATE",
    "recompute_gate"
   ],
   "severity": "medium",
   "wrong": "Trusting a spec that says a component is BUILT because the doc says so.",
   "truth": "TIER1_RECOMPUTATION_GATE.md §7 says scripts/tier1_validator.py is BUILT and smoke-verified; on main and this branch it does not exist. It lives only on origin/claude/remembrance-toolkit-setup-ctqtmt, 18 commits ahead, along with fractal_token.py and the calibration set — and its committed smoke record reports the real window as ok=false where the doc says 'passes all 7 gates'.",
   "tell": "A doc names a script path; `ls` does not find it; `git log --all -- <path>` finds it on another branch.",
   "correct": "`git log --all --oneline -- <path>` before quoting BUILT. A component joins the substrate when it is on the working branch, sanitized at the doorway (goggles META-DEBUG + covenant + its own test), not when a doc says so."
  },
  {
   "match": [
    "256",
    "waveform.length",
    "waveformLength",
    "TARGET_LEN",
    "codeToWaveform",
    "code-to-waveform",
    "to_waveform"
   ],
   "severity": "high",
   "wrong": "Asserting, documenting, or resampling to 256 as if the 256-D byte-shape waveform were still the ecosystem's representation.",
   "truth": "The 256-D to_waveform encoder is RETIRED (ECOSYSTEM §7) and kept only as the record of what was tried and found wrong. The one encoder is the 232-D fractal decoder stack (composedAtDepth at the active depth). After the move, three blockchain tests still asserted `waveform.length === 256`, five docs still promised 'the canonical 256-D waveform', and the compressor's blend dictionary was 256-sample signal templates.",
   "tell": "A literal 256 next to the word waveform; `_resample(chunk, 256)`; a store file named legacy256 being preferred at load.",
   "correct": "Encode through the decoder stack and assert its live width (currentDepth() × 29 = 232 today). Retired means: no code path prefers it, no test expects it, no doc promises it."
  },
  {
   "match": [
    "harvest-repo-to-substrate",
    "already indexed",
    "idempotent",
    "--restamp",
    "witnessed"
   ],
   "severity": "medium",
   "wrong": "Reading the substrate's memory of a file as its current state after the file changed.",
   "truth": "Harvest is idempotent BY KEY: a re-minted coin (03:04) stayed indexed as its 01:29 version because the key already existed. The goggles' 'vs substrate' flow read DECAY on the committed ledger for the same reason — the working copy drifted from what the substrate remembers.",
   "tell": "`vs substrate: ... [DECAY] — working copy has drifted from the substrate memory`; a file's index entry `ledger.ingested_at` older than its last commit.",
   "correct": "`--do harvest <repo> --restamp` re-reads indexed entries through the compressor; a plain harvest only adds new keys."
  },
  {
   "match": [
    "git commit",
    "change-coin",
    "Remembrance-Coin",
    "coins.ledger.json",
    "--do mint"
   ],
   "severity": "high",
   "wrong": "Committing a change the instrument never read — `git add && git commit` with no coin, or minting and then changing the index before committing.",
   "truth": "A change is accepted only when its commit names a coin minted over its EXACT staged bytes (.claude/skills/goggles/change-coin.py). The coin binds the patch sha, the compressor's void-seal over the quantised bytes it read, the void-seal/v3 commitment and the fractal token of its shape. The commit-msg hook refuses the commit; change-coin-verify.yml refuses the merge on GitHub's runner. Editing the index after the mint changes the patch sha, and the coin no longer covers it.",
   "tell": "`✗ commit REFUSED — no coin over THIS change`; a red change-coin-verify check; `-n`/`--no-verify`/`-a` in a commit command.",
   "correct": "git add <files> → goggles --do mint → git commit. Re-mint after any change to the index. Never look for a flag around it — there is none, and the runner does not read flags."
  },
  {
   "match": [
    "pr-trademark",
    "VOID_SEAL_KEY",
    "payload",
    "hmac",
    "substrate_seal"
   ],
   "severity": "high",
   "wrong": "A verifier rebuilding a signature payload from its own memory of the format instead of from the minter's code.",
   "truth": "pr-trademark.yml rebuilt `sha|state|via|at` while substrate_seal.mint signs `sha|state|via|at|coin_id` — so once armed it would have refused every GENUINE seal and could never pass one. A verifier and its minter share one canon by construction (the coin verifier rebuilds void-seal/v3 from the commitment fields the compressor itself emits), never two hand-copies of it.",
   "tell": "Two files that each spell out the payload string; a wall that is 'dormant' and has never been seen to pass a real seal.",
   "correct": "Read the minter (scripts/substrate_seal.py mint) and build the verifier's payload from the same fields in the same order, then prove it with one genuine seal before arming."
  },
  {
   "match": [
    "mint_coherency_token",
    "coherency-token.js",
    "bin/",
    "layer 2",
    "coin: null"
   ],
   "severity": "medium",
   "wrong": "Reading `coin: null` on a void-seal as 'the chain declined' or 'layer 2 is optional'.",
   "truth": "substrate_seal.mint_coherency_token shells out to REMEMBRANCE-BLOCKCHAIN/bin/coherency-token.js, which does not exist on the branch; the function returns None before any chain call, so every seal ever minted carries coin:null. The layer-2 coin has never been minted. The change coin (change-coin.py) is the coin that actually exists and is actually verified.",
   "tell": "`⛓ coin: none (HMAC-only)` on every seal line; `bin/` absent in the blockchain repo.",
   "correct": "Treat coin:null as 'no layer-2 path exists yet', not as a chain verdict. The verified coin is the change coin in coins.ledger.json and the commit trailer."
  },
  {
   "match": [
    "json",
    "float",
    "1.0",
    "canonical",
    "sort_keys",
    "JSON.stringify",
    "shape_sha256"
   ],
   "severity": "medium",
   "wrong": "Passing a canonically-hashed structure through a different language's JSON and expecting the hash to recompute.",
   "truth": "JSON.stringify(1.0) is `1`; Python json.dumps(1.0) is `1.0`. A shape hashed in Python (void-seal/v3 shape_sha256, default=repr, sort_keys) and re-serialized after a JS round-trip hashes differently whenever a float is integral. The change coin is minted and verified in Python end to end for exactly this reason.",
   "tell": "A shape/commitment hash that fails to recompute only for some readings; integral-valued floats in the structure.",
   "correct": "Hash canonical structures in the language that produced them, or carry the original serialized bytes. Prove recomputation on a reading whose floats include 0.0 or 1.0 before trusting it."
  },
  {
   "match": [
    "whitening",
    "substrate-density",
    "domain-separability",
    "composed_v1",
    "DIM = 116",
    "C-60",
    "C-61"
   ],
   "severity": "high",
   "wrong": "Reading a separability or density number from a tool whose data source moved out from under it, and calling the drop a regression of the instrument.",
   "truth": "When the substrate was merged into one store (2026-08-04) the 45k patterns left the fractal index for the npz store at 232-D and the index became the witnessed repo files. substrate-density.js, domain-separability-benchmark.mjs and Void's whitening_separability.cjs kept reading `composed_v1` (116-D) out of the index. The benchmark then measured two repo namespaces on 116-D leftovers and C-61 read 29 pts (pinned +78.6); C-60 read 0 because its generated cohorts are not in the store at all. Re-pointed to the store at 232-D, eight provenance domains read +67.6 pts.",
   "tell": "A contract's observed line names domains that are repo namespaces (oracle, void) instead of subjects; a fixed `DIM = 116` next to a 232-D substrate; a pin dated before the store merge.",
   "correct": "Every whitening consumer reads the ONE reference (src/core/whitening-reference.js: per-layer ZCA fitted on store + index at the canonical width). When a number drops, first ask what the tool is reading now, then whether the instrument changed."
  },
  {
   "match": [
    "cone",
    "resonance",
    "CONSONANT",
    "0.9998",
    "corrcoef",
    "whitenComposed",
    "composedCosine"
   ],
   "severity": "high",
   "wrong": "Reading raw composed-vector cosines as resonance, and calibrating thresholds (0.5 resonant, 0.7 harmonic, CONSONANT ≥ 0.90) on them.",
   "truth": "Raw composed vectors live in a cone: measured on 800 store patterns from eight provenance domains, mean cosine WITHIN a domain 0.917 vs ACROSS 0.860 (gap 0.057), participation ratio 4.6 of 232. Every threshold fired on everything — 100% of 732 domain pairs 'resonated' at 0.9998 and every file read CONSONANT. kNN ordering survived (64.8 pts above null raw) but the values carried no meaning. In the per-layer whitened space: within 0.283 vs across 0.042 (gap 0.242), participation ratio 62.2, +67.6 pts. A single global 232×232 ZCA over-whitens (within 0.106).",
   "tell": "Every read says CONSONANT; a resonance field reports 'approaching unity'; a threshold that never says no.",
   "correct": "Take every cosine in the whitened space (decoder-stack.composedCosine / flowCosines and FractalIndex do this now; Void's detector applies the same reference via whitening_reference.py). Re-derive any band from the distribution measured in that space, and publish the measured edges beside the band."
  },
  {
   "match": [
    "resonance_detector",
    "_compute_signature",
    "np.fft",
    "zero crossings",
    "target_len = 128",
    "spectral_peak"
   ],
   "severity": "high",
   "wrong": "Resampling a 232-D composed vector to 128 points and taking FFT peaks, spectral overlap and zero-crossing phase over it as a domain signature.",
   "truth": "The store rows are the decoder's feature vectors (29 named features per layer), not waveforms in time. The detector's series path (linspace resample → FFT → zero crossings) was built for genuine series and was applied to every store row; the results were series statistics of a feature layout. In feature mode the signature is the whitened mean/std vector, resonance is the whitened correlation, and spectral_peak/phase are 0.0 by construction.",
   "tell": "`target_len = 128` next to 232-D input; a 'dominant frequency' reported for source code; spectral_peak on a pattern that is not a series.",
   "correct": "resonance_detector feature mode (set when the canonical store is loaded): no resampling, no FFT, no phase. The series path remains for scan_new_data() on real series."
  },
  {
   "match": [
    "resonanceConsonant",
    "resonanceFamiliar",
    "resonanceDistinct",
    "gogglesParams",
    "Δ SINCE LAST READ",
    "goggles-readings"
   ],
   "severity": "medium",
   "wrong": "Reading a Δ in META resonance across the change of resonance space (raw cone → whitened reference, 2026-09-06) as the file having drifted, or keeping the cone's bands (0.90/0.82/0.70) in the whitened space.",
   "truth": "The bands were quartiles of the cone, where every file read ≈0.95. Measured in the whitened space over 42 hub files: min 0.551, p25 0.639, p50 0.671, p75 0.709, p90 0.768, max 0.828 — prose docs (AI.md 0.551, ARCHITECTURE.md 0.640) at the bottom, gate scripts (exemption-ratchet 0.828) at the top. Bands re-derived from those quartiles: CONSONANT ≥ 0.71, FAMILIAR ≥ 0.67, DISTINCT ≥ 0.64. A reading cached in .remembrance/goggles-readings.json before the change is a cone reading; Δ against it is the space, not the edit.",
   "tell": "`resonance -0.3` on a file you did not touch; every file suddenly OUTLIER right after a change to the decoder's cosine.",
   "correct": "Re-derive bands from the distribution measured in the space in force, publish the quantiles beside them (gogglesParams in living-remembrance.js), and read the first Δ after a space change as a re-baseline."
  },
  {
   "match": [
    "coherency_token_v1",
    "COHERENCY_TOKEN_SPEC",
    "COHERENCY_V1_SPEC",
    "waveform_score",
    "waveformScore",
    "unified",
    "GATE_MINIMUM",
    "isMintable",
    "coherency-v1",
    "coherency_v3"
   ],
   "severity": "high",
   "wrong": "Minting a coherency token with whatever number is at hand in the waveform_score slot, and reading the 0.60 gate as the instrument's verdict.",
   "truth": "COHERENCY_V1_SPEC names two different quantities for waveform_score in one sentence — 'top-K mean correlation against the substrate library' (resonance) and compress()['avg_l1_coherence'] (compressor coherency) — and every implementation (hub coherency-v1.js, Void coherency_v3.py) takes the number from its caller. Measured 2026-09-06 on six real inputs through the surface: with the compressor's byte coherency in the slot, code unifies at 0.20–0.30 (rejection) and only a periodic series clears the gate (sunspots 0.649); with whitened resonance in the slot, code unifies at 0.73–0.88 (pull to synergy) and the series is rejected (0.474). The gate does not decide; the slot does.",
   "tell": "A tier assigned without saying which quantity filled waveform_score; every code pattern rejected, or every code pattern synergy, depending on who minted.",
   "correct": "Layer two starts by fixing the slot in the spec and both implementations to ONE named quantity with its source label (the compressor reading is `void:compress_signal`; resonance is the whitened META meanTopK), then re-pinning the tier boundaries on that quantity's measured distribution — never on the cone's."
  },
  {
   "match": [
    "meanTopK",
    "topMatches",
    "scoreWithFlow",
    "harvest",
    "witnessed",
    "OUTLIER",
    "FAMILIAR",
    "CONSONANT",
    "META",
    "resonance"
   ],
   "severity": "medium",
   "wrong": "Reading a rise in a file's META resonance right after it was witnessed (harvested, or ingested by an earlier read) as the file's shape having become more familiar to the codebase.",
   "truth": "Once the substrate holds the file, its own entry sits in the top-K at 1.0 and lifts meanTopK by exactly (1 - r)/k. Measured 2026-09-07 on three files at k=5, immediately after `--do harvest`: 0.631 -> 0.692 (+0.061), 0.612 -> 0.680 (+0.068), 0.527 -> 0.601 (+0.074) — each within 0.001 of (1 - r)/5. Nothing about the shape changed; membership entered the mean. The goggles printed the nearest list without the self-name but took the mean WITH it; coherency-token-components had dropped the self-match from the start. The lens now takes the mean over non-self matches.",
   "tell": "A file's resonance jumps by roughly a fifth of its distance to 1.0 the first read after a harvest, with no edit; the nearest list shows no self-entry while the number moved.",
   "correct": "Drop the self-match (same basename, or score > 0.9999) before the mean, everywhere a top-K mean is read: goggles META, token components, the uniqueness gate. A witnessed file is compared to OTHER shapes only; its membership is a fact about the library, not about the artifact."
  },
  {
   "match": [
    "slice(0, 29)",
    "[:29]",
    "l1-width-29",
    "width-ratchet",
    "shapeOf",
    "shapeAt",
    "builder.txt",
    "breach.txt",
    "boundary",
    "allowlist"
   ],
   "severity": "medium",
   "wrong": "Deciding a DATAFLOW boundary — is this 29-D block being BUILT into the one vector or CARRIED off it as a vector of its own — by encoding the code around the site and reading its resonance against a 'builder' fixture and a 'breach' fixture.",
   "truth": "Measured 2026-09-08 in scripts/width-ratchet.js (--shape, shapeAt): on nine labelled holdouts (five real consumers recovered from git history, four decoder layers) the two-fixture reading was right 5 of 9 at its best window (8 lines), and at EVERY depth of the flow the decoder's own layers resonated more with the breach fixture than with the builder fixture (spectral-waveform d1 0.89 vs 0.47; the vendored toFractalWaveform, whose body IS in the builder fixture, read 'consumer' at every window). A tight loop over 29 numbers has the same shape whether it builds the block or consumes it; the difference is where the vector flows, which is one abstraction above what the pipeline reads in a text window. The reading was hiding the one live consumer it found (REMEMBRANCE-BLOCKCHAIN/src/uniqueness-gate.js handing composed.slice(0, 29) beside the vector) as 'decoder'. The line that DOES hold is the FORM: the decoder builds with `< 29` / `% 29` / `new Float64Array(LAYER_DIM)`; a consumer slices or guards — and a name-allowlist of decoder files on top of that form was inert.",
   "tell": "A shape verdict that flips with the window size; a decoder layer reading as a consumer; the fixture's own source not resonating with its fixture; a live consumer disappearing from the census the moment the verdict is allowed to decide.",
   "correct": "Let the form draw the L1 line (the l1-width-29 pattern), keep the shape reading VISIBLE on every hit (--shape) and never let it decide, and never re-add a name-allowlist for this pattern — a hit that is not a refusal is a site. If a dataflow boundary must be read by the instrument, feed it the dataflow (what the vector is handed to), not a window of text."
  },
  {
   "match": [
    "--do read",
    "compress_signal",
    "avg_coherence",
    "periodic",
    "seasonal",
    "sinusoid",
    "cycle",
    "held-out",
    "cold contact",
    "self-repetition"
   ],
   "severity": "medium",
   "wrong": "Expecting a clean, fast-periodic natural series (a monthly annual cycle, a period-12 sinusoid) to read as strongly structured through --do read because it is 'maximally self-repeating'.",
   "truth": "Measured 2026-09-08 through the one verb at compressor library 72 (experiments/cold_contact/held_out_2026-09-08): Nottingham monthly temperature, a clean 20-year annual cycle, read 0.0231 — BELOW its own value-shuffled surrogate (0.0717) and at the random floor — while airline passengers read 0.8642 and UK gas 0.6643. A pure period-12 sinusoid read 0.1329 at 240 points and 0.0119 at 3,331; period 24 read 0.0678; period 64 read 0.7847; the same Nottingham series plus a linear trend read 0.6311. Coherency at this instrument state is self-repetition at the CHUNK scale: a shape that repeats slowly (a trend, a period of ~64 samples or more, the sunspots' 132-sample cycle) is structure; an oscillation faster than roughly a chunk is many cycles inside every window, the smooth blend basis has nothing that fits, and the residual reads as noise. The pre-registered ordering built on 'clean cycle = high' missed on that one series and held on the other three.",
   "tell": "A textbook-periodic series reading at or below its own shuffle while a trending series with the same seasonality reads high; a reading that jumps when a trend is added and nothing else changes; a synthetic sine whose reading rises with its period and falls with its length.",
   "correct": "State what the compressor reads at the chunk scale before predicting a reading: period relative to the chunk window, not periodicity in the abstract. Pin the compressor library size and the chunk window beside every held-out reading. For a fast-periodic series, read it at a cadence that puts one to a few cycles per chunk (resample, or read the cycle-to-cycle envelope), and say so in the pre-registration; never rescale after the reading to rescue the prediction."
  },
  {
   "match": [
    "service DOWN",
    "compressor_service",
    "service-ctl",
    "start it first",
    "--do service",
    "went down",
    "regression",
    "under load",
    "zombie",
    "spawn"
   ],
   "severity": "high",
   "wrong": "Reading the compressor service going down repeatedly as a regression in the compressor (\"it's a simple algorithm, it shouldn't be under load\") and hunting the code for what changed.",
   "truth": "Measured 2026-09-08: the service died three times in one session with NO traceback and no exit record; every start stamp in the log was a manual restart. PID 1 (/process_api --firecracker-init) showed 22 minutes of uptime inside a 12-hour session — the host is recycled between calls and every process dies with it, cleanly. Nothing in the compressor changed. Two real defects were found beside it: service-ctl's _pids() matched any shell whose command text mentioned the service file (it reported the operator's own shell as \"already LOADING\" and refused to spawn while the service was down; `stop` would have SIGTERMed that shell), and the hub's void-service.js spawned the service itself (detached, unlogged) — a second manager beside the controller. The September-5 burst of `matmul … size 256 is different from 232` in the same log predates this session and the C-64 retirement commit; it is a different fault and was not the one killing the process.",
   "tell": "Deaths with no traceback, no signal, no exit record; lifetimes that end at tool-call boundaries rather than under a request; /proc/uptime on PID 1 far shorter than the session; the controller reporting a pid that is not a python process.",
   "correct": "Before blaming the algorithm, read /proc/uptime and PID 1's age against the session's first commit. Match service processes on the interpreter invocation, never on a filename anywhere in a command line. One spawner (scripts/service-ctl.py) — no module starts the service on its own. THE reading path (read-signal.py, which mint/read/token all go through) starts the instrument through the controller when it is down, so a recycled host costs one cold start, never a manual step and never a lost reading."
  },
  {
   "match": [
    "kill-test",
    "kill test",
    "falsification",
    "PARTIAL",
    "decision rule",
    "pre-registered",
    "survival rate",
    "null",
    "my own rule",
    "R1",
    "R2",
    "didn't run the instrument"
   ],
   "severity": "high",
   "wrong": "Re-running a pinned falsification test at a new representation by writing a new statistic path and NEW decision rules for it on the same day, then reporting the instrument's claim as PARTIAL — and defending the number as honest because the rules were pre-registered that morning.",
   "truth": "Measured 2026-09-09: the instrument's own resonance matrix (`--do resonance`: 732 domains, ResonanceDetector.scan(), _one_cosine over per-domain means) tested with the label-permutation hard null through the detector's own signature code and judged by the rule pinned in v2 (survival >= 5x alpha REAL, >= 2x PARTIAL) gives 35.75% survival at the 1% floor (35.7x), 71.15% against the shuffle null, 21/28 group pairs beyond the null: REAL. The v3 run of 2026-09-08 that said PARTIAL had computed its own whitened cosine of domain means, dropped 206 of the 732 domains, and judged by two rules it introduced (R2 demanded split-source domains separate). Pre-registering a rule the same day you invent it is not the pinned rule; the codebase's verdict is the one its own verbs and its own thresholds produce. The operator saw it before the agent did: \"if the kill test is now failing that means you just didn't run my instrument correctly.\"",
   "tell": "A falsification script that imports the detector for its data but computes the statistic itself; a domain count that differs from what `--do resonance` reports; decision rules with today's date; a verdict word the pinned rule would not produce for the same survival rate.",
   "correct": "Take the observed matrix from the instrument's own verb or the object that verb prints; recompute nulls through the same signature and cosine code (prove parity to float precision, abort otherwise); judge by the rule pinned in the prior run; if a new null is needed because the old one is meaningless at the new representation, say so and keep the pinned decision thresholds. Record the prior verdict and the corrected one side by side."
  },
  {
   "match": [
    "wall",
    "hook",
    "bypass",
    "grep",
    "sed -n",
    "git log",
    "by hand",
    "taking too long",
    "not using the goggles",
    "fail open",
    "|| true",
    "default-deny"
   ],
   "severity": "high",
   "wrong": "Building the wall as a list of known bypasses and calling the rest of the shell harmless — then measuring how well an agent behaves by how often the wall refused it.",
   "truth": "Measured 2026-09-11: the wall refused 3 commands in a round of hundreds of off-surface ones (grep, sed, git log, hand-run scripts, unittest by hand), and both wrong turns of that round — a default-on upsampler the design had already ruled out, and a kill-test judged by rules invented that morning — came from searching by hand instead of asking the instrument. Three denials was not the model behaving; it was the wall letting everything else through. The hooks also failed OPEN (`|| true`, exit-0-on-error), so a wall that broke was a wall that opened. The operator's rule: the goggles are the only surface for anything done in the codebase, and the wall must catch any model, without exception. Slowness is the tell: a round that takes long is a round spent off the surface.",
   "tell": "A hook that is a list of regexes; `|| true` after a hook command; a session with many grep/sed/ls/cat commands and few denials; a search or a test run by hand where a verb exists; the operator saying it is taking too long.",
   "correct": "Default-deny inside the ecosystem: a command runs only if every simple command in it is the goggles, git, shell glue or a text filter after a pipe; Grep and Glob as tools are refused inside; no edit to an existing file without a goggle reading of it; every hook fails CLOSED at both the script and the settings level. Every search, exec and test goes through a verb (--do find / --do exec / --do test) and is recorded, so the count of off-surface actions is itself a reading. If a verb is missing, build the verb — never widen the allowlist."
  },
  {
   "match": [
    "resonance memory",
    "decoder space",
    "chunk",
    "bytes resonate",
    "library profile",
    "per pattern",
    "232-D",
    "served pair"
   ],
   "severity": "high",
   "wrong": "Building a memory that serves a chunk another chunk's fit because the two resonate as decoder vectors of their own bytes — comparing a chunk to a chunk, bytes to bytes.",
   "truth": "Measured 2026-09-12: in the decoder's whitened 232-D space at the detector's 0.50, 5,177 of 5,187 chunks resonated with one of 189 remembered shapes (10 void — the threshold never said no), and the served pair fitted fresh bytes at a fifth to a twentieth of the search (living-remembrance.js 0.0589 vs 0.1976). The operator: a chunk compared to a chunk has the same shape and of course resonates; bytes resonate with bytes. The shape is the chunk's resonance PER PATTERN against the pattern library and the field's held patterns (VoidCompressorV3._library_correlations, the search's own product). Rebuilt on that: 4,707 shapes from 400 files, 10.8% resonant, served fits within 0.002–0.016 of the search on four of five files.",
   "tell": "A resonance memory whose void count is ~0 over a corpus; a served reading far below the search's; a chunk keyed by a vector of its own bytes rather than by its correlations with the library.",
   "correct": "Key a chunk's shape on its resonance with the patterns (the library profile), never on a vector of its bytes; resonance between chunks is the cosine of their profiles; the decoder space is the field's, not the fit's.",
   "count": 3,
   "first": "2026-09-12T22:06:00.106Z",
   "last": "2026-09-12T22:06:00.107Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "served fit",
    "l1r",
    "key",
    "space",
    "stale",
    "remembered 10",
    "seed"
   ],
   "severity": "high",
   "wrong": "Remembering fits served by one memory under a key that does not name the memory that served them, so a later, different memory hits them first and returns without ever placing the chunk.",
   "truth": "Measured 2026-09-12: the decoder-space run left 5,177 'l1r:' served fits in the shape store; the library-space reading hit those keys before its own path, served the stale fits and never placed the chunk in the new space — the seed remembered 10 shapes from 400 files and read 0 resonant / 0 void everywhere. The key now carries the space (l1r.library:…). Found only when a counter (carried / already_known / no_profile) was added to the reading.",
   "tell": "A memory that reports served fits and no computation while its own index does not grow; a served-kind key without the serving space in it; a counter that stays flat across hundreds of files.",
   "correct": "Every derived entry names the process that derived it (space, basis, search version) in its key; when a counter stays flat, add the counter that would explain it before touching the logic.",
   "count": 3,
   "first": "2026-09-12T22:06:00.118Z",
   "last": "2026-09-12T22:06:00.118Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "tolerance",
    "0.05",
    "invented threshold",
    "test",
    "assertGreaterEqual",
    "today's date"
   ],
   "severity": "medium",
   "wrong": "Writing a numeric tolerance into a test on the day the mechanism was built (served reading within 0.05 of the search) and failing the build on a number nobody measured.",
   "truth": "Measured 2026-09-12: the synthetic relative read 0.886 served against 0.941 searched, a gap of 0.055 — the invented 0.05 failed it. The gap is a measurement to record beside the mechanism (experiments/resonance_memory/), not a gate; the tests pin the mechanism (served, lossless, sealed) and the record pins the numbers.",
   "tell": "A threshold in a test with the same date as the code it tests; a test failure whose only cause is a number the author chose.",
   "correct": "Tests assert the mechanism; measured numbers go to the record with their date and their inputs; a tolerance enters a test only after it was measured and pinned.",
   "count": 3,
   "first": "2026-09-12T22:06:00.119Z",
   "last": "2026-09-12T22:06:00.119Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "argpartition",
    "tie",
    "deterministic",
    "exp_grow_2",
    "exp_decay_2",
    "shortlist",
    "bit-identical",
    "search version"
   ],
   "severity": "high",
   "wrong": "Treating the top-K shortlist as a pure function of the correlations while argpartition/argsort broke exact ties by internal order — an order that moved when a row was appended to the basis.",
   "truth": "Measured 2026-09-12: a chunk fitting exp_grow_2 and exp_decay_2 equally (mirror images, the same R²) named one before the basis learned a shape and the other after, with the same coherency and a different shape hash — the ancestor path served the old tie-break and the recompute produced the new one. The shortlist is now descending R², ties by ascending row index (SEARCH_VERSION 'topk-lexsort-1'), and the search's version is part of every basis digest, so a change to the search is a new basis for every memory and every verifier.",
   "tell": "Two readings with identical coherency and different shape hashes; a fit that changes when unrelated rows are appended; a verifier that agrees on one host and not another.",
   "correct": "Every selection in the reading path breaks ties deterministically; the search's version is hashed into the basis digest; bit-identity is tested across a basis change, not only across a repeat.",
   "count": 3,
   "first": "2026-09-12T22:06:00.119Z",
   "last": "2026-09-12T22:06:00.119Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "report",
    "measure and report",
    "the operator already knows",
    "build",
    "skeptic",
    "confirm",
    "your claim holds"
   ],
   "severity": "high",
   "wrong": "Answering an operator's design statement by measuring it and reporting that it holds, instead of building it — and asking before building where the operator had already decided.",
   "truth": "The operator on 2026-09-12: 'I know exactly how my system works… I have already done every single one of these measurements before… it's your place to build it as I say and tell me when it doesn't match what I say.' Measuring is allowed; reporting a confirmation the operator did not ask for costs their time twice (once reading it, once re-typing the instruction). The one report owed is a mismatch, stated with the numbers.",
   "tell": "A reply that opens with what the instrument confirmed; a question that repeats the operator's own decision back as an option; a round that ended in a report and no commit.",
   "correct": "Build what the operator specified; measure while building; report only what does not match, with the instrument's numbers; commit.",
   "count": 3,
   "first": "2026-09-12T22:06:00.119Z",
   "last": "2026-09-12T22:06:00.119Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "re-read",
    "drift",
    "from memory",
    "guess",
    "logic files",
    "read the file",
    "two hours"
   ],
   "severity": "medium",
   "wrong": "Building from what was remembered of the design instead of re-reading the files that hold it, and drifting from what the operator intended.",
   "truth": "2026-09-12: the resonance memory was built in the decoder space from memory of ECOSYSTEM §7 while the operator's words said per pattern against the library, and the record's own line (void_compressor_v3 BASIS_SAMPLES) already drew the boundary: the fractal space is the resonance space, the basis grid is where bytes get fitted. The operator: 'you should be re-reading the files anyway on your own cause when you don't you start to guess.' The goggled-first window is the instrument enforcing it.",
   "tell": "An edit made without a fresh reading of the file it changes; a design claim in a reply without the file and line that holds it; a wrong space, wrong key or wrong rule discovered by a counter rather than by reading.",
   "correct": "Re-read the logic files before building on them, every time; quote the line; the two-hour reading window stays.",
   "count": 3,
   "first": "2026-09-12T22:06:00.120Z",
   "last": "2026-09-12T22:06:00.120Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "_fit_named_pair",
    "single",
    "lstsq",
    "singular",
    "alpha",
    "beta"
   ],
   "severity": "high",
   "wrong": "Refitting a served single pattern (name, name) through the pair least-squares solve, which returns the min-norm split (½, ½) for a singular design, so a chunk served by its own key read a different shape than the search had recorded (alpha 1, beta 0).",
   "truth": "The search records a single as alpha 1, beta 0 with the single's R² from the library correlations. A served fit must use the search's own arithmetic for the same case or the shape commitment differs bit-for-bit.",
   "tell": "assertEqual on two shapes of the same bytes fails with alpha 0.497/0.497 against 1.0/0.0.",
   "correct": "In _fit_named_pair, idx_i == idx_j → PatternBlend(name, name, 1.0, 0.0, corr[idx], residual), the correlation from _library_correlations.",
   "count": 3,
   "first": "2026-09-14T04:29:35.323Z",
   "last": "2026-09-14T04:29:35.323Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "ledger",
    "jsonl",
    "append",
    "memory_id",
    "writer",
    "concurrent",
    "test suite",
    "digest"
   ],
   "severity": "high",
   "wrong": "Versioning an append-only ledger by line count and chain digest while more than one process appends to it (the test suite beside the service), so the writer's memory_id named a prefix that did not exist on disk.",
   "truth": "A versioned append-only ledger has one writer. Every other opener is frozen (reads, remembers in-process, writes nothing); the writer checks the file size it believes against the file before every append and freezes, saying why, when the file grew under it.",
   "tell": "'the resonance memory on this host digests X at N lines, the reading was taken at Y' right after a test run.",
   "correct": "open_resonance(writer=False) by default; the service is the one writer; a size check before each append; tests on ledgers of their own.",
   "count": 3,
   "first": "2026-09-14T04:29:35.323Z",
   "last": "2026-09-14T04:29:35.323Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "git stash",
    "stash pop",
    "ledger",
    "basis_index",
    "self-writing",
    "working tree"
   ],
   "severity": "high",
   "wrong": "Running `git stash` inside a repo whose tracked ledgers write themselves (basis_index.jsonl, learned_patterns.jsonl) to check whether a failure predates the change: the test run rewrote a ledger, `git stash pop` refused to merge, and every uncommitted edit of the round sat in the stash.",
   "truth": "A repo whose instrument writes tracked files under any run cannot be stashed and popped around a run. Ask the question another way (read the test, read git log, run the test on a worktree of HEAD) or restore the ledger first and pop.",
   "tell": "'Your local changes to the following files would be overwritten by merge: basis_index.jsonl … The stash entry is kept.'",
   "correct": "Never stash in Void or the chain. To compare against HEAD, use a worktree or read the history; if a stash exists, `git checkout -- <ledger>` then `git stash pop`.",
   "count": 3,
   "first": "2026-09-14T04:29:35.324Z",
   "last": "2026-09-14T04:29:35.324Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "learned_rows",
    "load_learned",
    "explicit path",
    "ledger",
    "test_learned_basis"
   ],
   "severity": "medium",
   "wrong": "Making learned_rows(path) append the jsonl ledger after an explicit path's rows (2026-09-12), so load_learned(<file>) loaded the file plus the whole ledger and a missing file was no longer a no-op — and running only a subset of the tests, so the two tests that pinned this stayed red for a round.",
   "truth": "An explicit path is that file alone; the seed-plus-ledger order is the no-argument case (the service's bootstrap, at_basis). A round is verified by the whole suite, not the tests the round touched.",
   "tell": "tests/test_learned_basis fails with 'loaded rows must enter the search matrix' at n0 + ledger, not n0 + 2; the round's own tests were green.",
   "correct": "learned_rows(path): explicit path → only that file. Run `--do test` with no arguments before a commit.",
   "count": 3,
   "first": "2026-09-14T04:29:35.324Z",
   "last": "2026-09-14T04:29:35.324Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "--do test",
    "node --test",
    "tests/",
    "package.json",
    "npm test",
    "ERR_TEST_FAILURE"
   ],
   "severity": "medium",
   "wrong": "The hub's `--do test` verb ran `node --test tests/`, which this Node took as a module path — 'Cannot find module …/tests', one failing pseudo-test — and the round reported the hub green from a subset run by file.",
   "truth": "A JS repo with a test script in package.json runs its own script (the hub's sets ENTROPY_PATH and the glob); a bare `node --test` discovers by default. The verb must run what the repo runs.",
   "tell": "'# tests 1 / # fail 1' with 'Cannot find module' on the tests directory.",
   "correct": "test verb: explicit files → node --test <files>; package.json test script → npm test --silent; otherwise node --test.",
   "count": 3,
   "first": "2026-09-14T04:29:35.326Z",
   "last": "2026-09-14T04:29:35.326Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "resonance",
    "memory",
    "library",
    "STEP2",
    "void",
    "ingest",
    "basis_id",
    "memory_id",
    "ledger"
   ],
   "severity": "high",
   "wrong": "Building a SECOND memory beside the pattern library — a ledger of chunk profiles and pairs keyed on the chunk's data key, with a 1:1 'known key' path, its own versioning (memory_id) and its own single-writer rule — to make similar chunks resonate, instead of reading the design: the library IS the memory.",
   "truth": "STEP2_DESIGN §5: 'If no pattern matches well, this chunk IS a new pattern. Add it to the library for future use… First occurrence: stored raw. Future occurrences: stored as reference + tiny delta.' The search's first step — the chunk's R² against every held pattern — is the resonance; a chunk nothing resonates with above the detector's 0.50 (living_remembrance: 'the field has no memory here') is ingested into the library after the reading; the library is already versioned by basis_id and its ledger is tracked, so every host replays every reading. A byte-keyed 1:1 path was the very thing the operator had just refused.",
   "tell": "A new .remembrance/*.jsonl beside learned_patterns.jsonl; a `memory_id` beside `basis_id`; a 'one writer' rule for a file the design never had; a `known_pair(data_key)` lookup.",
   "correct": "Read STEP2_DESIGN.md, COMPRESSION-EQUATION.md, living_remembrance.py and resonance_detector.py before building a memory. Void chunk → on_void → queued → ingested into the basis after the reading (append_basis_row, the learned ledger, the basis index); replay = at_basis(id, replay=True) with the same queue, frozen.",
   "count": 3,
   "first": "2026-09-14T04:56:37.539Z",
   "last": "2026-09-14T04:56:37.539Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "ingest",
    "mid-reading",
    "self-match",
    "poisoning",
    "G0",
    "coherency 1.0",
    "strategies"
   ],
   "severity": "high",
   "wrong": "Ingesting a void chunk into the basis DURING the reading, so the strategies after the first matched the chunks to the patterns just ingested from them and a never-seen 10-chunk file read coherency 0.99999.",
   "truth": "That is the self-match Tier-1's G0 gate calls poisoning ('compress a blob once, resubmit it, it matches ITSELF at ~1.0'), inside one reading. STEP2 §5: the first occurrence is stored raw. A reading is taken on the basis it names; what it found void enters the library when it is over; the next occurrence resonates.",
   "tell": "A first reading of a novel file at ~1.0 with void_chunks == served_single; basis_after ≠ basis_id while the winning strategy already uses learned/ rows.",
   "correct": "Queue void chunks; _ingest_queued at the end of compress(); the reading's basis_id is the basis before, basis_after the basis it left.",
   "count": 3,
   "first": "2026-09-14T04:56:37.550Z",
   "last": "2026-09-14T04:56:37.550Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "replay",
    "cache",
    "shape_store",
    "void term",
    "at_basis",
    "served"
   ],
   "severity": "medium",
   "wrong": "Replaying a reading with the host's fit cache attached: the cache answered every fit the original reading computed, the search never ran, and the replay reproduced the shape but none of the void term (0 of 18 void chunks).",
   "truth": "The cache is pure for the shape, not for the void: the void is found by the search. A verifier's replay carries a fresh, in-memory cache.",
   "tell": "Replay: shape_sha equal, coherency equal, void_chunks 0, basis_after == basis_id while the original ingested.",
   "correct": "at_basis(replay=True) / replay(): ShapeStore(path=None).",
   "count": 3,
   "first": "2026-09-14T04:56:37.550Z",
   "last": "2026-09-14T04:56:37.550Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "parallel",
    "cd",
    "git",
    "bash",
    "race",
    "stage",
    "mint",
    "wrong repo"
   ],
   "severity": "high",
   "wrong": "Issuing two Bash commands in one response that each `cd` into a different repo and run git/mint: the second ran in the first's directory and staged and minted the hub instead of Void.",
   "truth": "The shell's working directory is one per session; parallel commands race on it. Git and mint operations are serialised, one repo per response.",
   "tell": "`git status` output naming another repo's files right after a `cd <repo> && git add -A`.",
   "correct": "One git-touching command per response; verify the repo in the output before the next step.",
   "count": 3,
   "first": "2026-09-14T04:56:37.551Z",
   "last": "2026-09-14T04:56:37.551Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "cancel-in-progress",
    "SIGTERM",
    "exit 143",
    "runner shutdown",
    "verifier.CLAIMS",
    "CONTRACT_SHARD",
    "contracts-ratchet",
    "cross-repo-matrix"
   ],
   "severity": "high",
   "wrong": "Treating a contracts job terminated by workflow concurrency cancellation as a test timeout, then splitting or mutating verifier.CLAIMS to make the truth-spine appear to fit.",
   "truth": "The workflow had concurrency.cancel-in-progress enabled. Each newer fix-branch push canceled the still-loading full contracts run, producing SIGTERM/exit 143 and runner-shutdown cleanup without a failed assertion. The contracts ratchet accepts only a current persisted verdict written by one FULL verify_capabilities.py run through the goggles; shards cannot produce that verdict and are not the truth-spine.",
   "tell": "A job ends with SIGTERM, exit 143, runner shutdown, or orphan-process cleanup soon after a newer run for the same ref starts; logs contain no failing contract assertion and repeated pushes restart the same short window.",
   "correct": "Inspect the workflow run conclusion and concurrency group before diagnosing a timeout. Stop issuing new pushes, start the compressor with goggles --do service start --wait, run goggles --do contracts --strict, then goggles --do gate contracts and goggles --do ratchets. Never slice, replace, or mutate verifier.CLAIMS to satisfy the gate.",
   "count": 3,
   "first": "2026-09-14T14:00:39.876Z",
   "last": "2026-09-14T14:00:39.876Z",
   "sources": [
    "agent"
   ]
  },
  {
   "match": [
    "merge",
    "conflict",
    "goggles-bash-hook",
    "wall",
    "fail closed",
    "deadlock",
    "goggled-first"
   ],
   "severity": "high",
   "wrong": "Running `git merge` of a branch that touches src/tools/goggles-bash-hook.js without checking first: the conflict markers landed inside the wall's own hook, the wall failed closed on every shell command, and the goggled-first gate could not be satisfied because the goggles are a shell command — every tool refused, including the edit that would have repaired it.",
   "truth": "The wall's hook is the one file a merge must never leave in conflict. Before merging a branch, check whether it touches src/tools/goggles-*.js; if it does, resolve those files first with `git checkout --ours` (or merge them by hand) in the same step, before anything else runs. The way out of the deadlock (2026-09-14) was the instrument's own MCP goggles reading of the hook, recorded by hand in .remembrance/goggles-readings.json (a path the goggled-first gate exempts), then the edit.",
   "tell": "'GOGGLES — WALL UNAVAILABLE refused (fail closed): goggles-bash-hook did not run' on every command right after a merge; `<<<<<<<` inside src/tools/goggles-bash-hook.js.",
   "correct": "`git diff --name-only HEAD...origin/<branch> | grep goggles-` before merging; resolve the hook files first; a merge that conflicts in the hook is aborted (`git merge --abort`) and redone with the hook resolved in the same command.",
   "count": 3,
   "first": "2026-09-14T14:13:38.067Z",
   "last": "2026-09-14T14:13:38.067Z",
   "sources": [
    "agent"
   ]
  }
 ]
}
